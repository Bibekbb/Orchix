server:
  host: "0.0.0.0"
  port: 8080

watch:
  enabled: true
  interval: 30s
  directories:
    - "./manifests"
    - "./orchix-manifests"

kubernetes:
  enabled: true 
  namespace: "default"
  kuberconfig: ""

logging:
  level: "info"
  format: "text"